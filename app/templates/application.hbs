{{#affinity-engine engineId="app" config=config fixtures=fixtures isFocused=true as |engine|}}
  {{#liquid-if (and engine.isLoaded sequencerComplete) class="liquid-engine"}}
    {{affinity-engine-stage engineId=engine.engineId initialScene="initial"}}
  {{else}}
    {{#affinity-engine-curtain engineId=engine.engineId filesToPreload=(hash keyframes="src" sounds="src") classNames="ae-full-size" as |curtain|}}
      {{#ember-sequencer
        animationAdapter="velocity"
        classNames="ae-full-size"
        frameDuration=1000
        animationIn=(hash duration=1000 effect=(hash opacity=(array 1 0)))
        animationOut=(hash duration=1000 effect=(hash opacity=0))
        onComplete=(toggle-action "sequencerComplete" this)
      as |sequencer|}}
        {{#sequencer.frame
          frameDuration=2000
          animationIn=(hash duration=1000 effect=(hash opacity=(array 1 0) translateY=(array 0 300)))
        }}
          <div class="true-center">
            <h1>You are Not a Dinosaur</h1>
            <div>Words by <a href="https://twitter.com/_nullnullnull_" target="_blank">null null null</a></div>
            <div class="margin-top-1">Image by <a href="https://www.instagram.com/jettiallen/" target="_blank">Jetti Allen</a></div>
            <div class="margin-top-1">Song 'Spacewolf' by <a href="http://freemusicarchive.org/music/Matt_Oakley/" target="_blank">Matt Oakley</a></div>
          </div>
        {{/sequencer.frame}}
        {{#sequencer.frame}}
          <a href="https://github.com/null-null-null/you-are-not-a-dinosaur" target="_blank" class="made-by-affinity">
            <div class="true-center">
              {{fa-icon "creative-commons"}}
              <div class="margin-top-3">This game is under Creative Commons.</div>
              <div class="margin-top-1">(click here to fork)</div>
            </div>
          </a>
        {{/sequencer.frame}}
        {{#sequencer.frame}}
          <a href="http://www.affinityengine.org" target="_blank" class="made-by-affinity">
            <div class="true-center">
              <img src="affinity-engine-logo.png">
            </div>
          </a>
        {{/sequencer.frame}}
        {{#sequencer.frame duration=500 as |frame|}}
          {{ember-progress-bar
            progress=curtain.progress
            useDefaultStep=true
            options=progressBarOptions
          }}
          {{#if frame.isVisible}}
            {{#ember-sequencer
              animationAdapter="velocity"
              animationIn=(hash duration=1000 effect=(hash opacity=(array 1 0)))
              animationOut=(hash duration=1000 effect=(hash opacity=0))
              classNames="ae-full-size"
            as |sub-sequencer|}}
              {{#sub-sequencer.frame}}
                <div class="true-center">
                  <div>Dinosaurs walked the Earth 65 million years ago.</div>
                </div>
              {{/sub-sequencer.frame}}
              {{#sub-sequencer.frame}}
                <div class="true-center">
                  <div>One of the largest dinosaur skeletons was 39 feet tall and 74 feet long.</div>
                </div>
              {{/sub-sequencer.frame}}
            {{/ember-sequencer}}
          {{/if}}
        {{/sequencer.frame}}
      {{/ember-sequencer}}
    {{/affinity-engine-curtain}}
  {{/liquid-if}}
{{/affinity-engine}}
