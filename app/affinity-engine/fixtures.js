import backdrops from '../affinity-engine/fixtures/backdrops';
import characters from '../affinity-engine/fixtures/characters';
import images from '../affinity-engine/fixtures/images';
import keyframes from '../affinity-engine/fixtures/keyframes';
import sounds from '../affinity-engine/fixtures/sounds';

export default {
  backdrops,
  characters,
  images,
  keyframes,
  sounds
};
